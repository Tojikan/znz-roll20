    <div class="sheet-inventory">
        <div class="sheet-slot-section">
            <div class="sheet-row-flex sheet-label-row">
                <label>{{data.character.inventory | getLabel}}</label>
                <div class="sheet-subtext">
                    Slots:
                    <input type="text" class="sheet-number sheet-shy" name="attr_{{data.character.inventory | getAttr}}" value="{{data.character.inventory.default}}"/>
                </div>
            </div>
            <fieldset class="repeating_inventory">
                <div class="sheet-slot">
                    {{ card.create("", "")}}
                </div>
            </fieldset>
        </div>
    </div> 